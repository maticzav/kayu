{
  "name": "ts-graphql-codegen",
  "version": "0.0.0",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn g:tsc --build && echo 'done build ts-graphql-codegen'",
    "prepack": "yarn build",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "camelcase": "^6.2.0",
    "graphql": "^15.4.0",
    "multilines": "^1.0.3",
    "node-fetch": "^2.6.1",
    "prettier": "^2.2.1"
  },
  "devDependencies": {
    "@graphql-tools/schema": "^7.1.2",
    "@types/node-fetch": "^2",
    "@types/prettier": "^2.1.6"
  }
}
